# ESP-IDF build artifacts
build/
sdkconfig
sdkconfig.old
dependencies.lock
managed_components/

# IDE files
.vscode/settings.json

# macOS
.DS_Store

# Credentials and secrets — never commit these
credentials*.h
credentials*.hpp
credentials*.cpp
secrets*.h
secrets*.hpp
wifi_creds*.h
*_credentials.*
*_secrets.*
# Kconfig local overrides that may contain real SSID/passwords
sdkconfig.local

